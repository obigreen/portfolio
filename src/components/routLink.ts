import {AccentAnimationCol} from "../styles/AccentAnimation";
import styled from "styled-components";

export const RoutLink = styled.a`
  display: inline-block;
  animation: ${AccentAnimationCol} 12s linear infinite alternate;
`